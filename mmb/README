## How to integrate mmb into vpp

	cp -r mmb src/plugins/mmb
	cp mmb/mmb.am src/plugins/mmb.am

Add the following entry to the plugins section of `src/configure.ac`.

	PLUGIN_ENABLED(mmb)

Add the following entry to the plugins section of `src/plugins/Makefile.am`

	if ENABLE_MMB_PLUGIN
	include mmb.am
	endif

Now (re)build VPP.

	$ make wipe
	$ make build
